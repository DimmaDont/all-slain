# pylint: disable=invalid-name

# Requires passing `--additional-hooks-dir=.` to pyinstaller

hiddenimports = [
    "data_providers.rsi",
    "data_providers.starcitizen_api",
    "data_providers.wks_navcom",
]
